startCommand:
    type: stdio
    configSchema:
        type: object
        properties: {}
        required: []
    commandFunction:
        |-
        (config) => ({
          "command": "node",
          "args": [
            "dist/index.js"
          ]
        })
